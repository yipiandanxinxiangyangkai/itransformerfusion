# itransformerfusion
### Prerequisites

Ensure you have **Python** installed on your system (Recommended version: e.g., 3.8+).

### Running the Program

To run the main application, navigate to the root directory of this repository and execute the `main_run.py` file:

```bash
python main_run.py
